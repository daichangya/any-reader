{
  "buildCommand": "pnpm install && pnpm build:docs",
  "outputDirectory": "docs/dist",
  "installCommand": "pnpm install",
  "framework": "vitepress",
  "engines": {
    "node": "18.x"  // ValidValid way to specify Node.js version
  }
}
