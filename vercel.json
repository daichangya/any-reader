{
  "buildCommand": "pnpm install && pnpm build:docs",
  "outputDirectory": "docs/dist",
  "installCommand": "apt-get update && apt-get install -y python3-distutils && pnpm install",
  "framework": "vitepress"
}
